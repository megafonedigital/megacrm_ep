# Copie para .env (ou configure direto no registro do MCP)
SUPABASE_URL=https://mynmvycwhfexwhyzxnzp.supabase.co
# Chave service_role — Supabase Dashboard → Settings → API → service_role (secret)
SUPABASE_SERVICE_ROLE_KEY=
# Opcional: base do app para disparar crons manualmente (run_broadcast_loop)
APP_URL=
